<!--
TEMPLATE_ID: VMPL-001
版本号: v1.0
适用范围: 验证实验报告 — 技术团队 / 执行方 / QA人阅读
作者: 墨枢系统
创建时间: <必填: ISO8601 +08:00>
revision_log:
  - v1.0 (2026-05-24): 首次发布
-->

# 验证实验报告

## 元信息

```json
{
  "template_id": "VMPL-001",
  "version": "1.0",
  "report_id": "<必填: 试验名_YYYYMMDD_HHMMSS>",
  "experiment_name": "<必填: 验证试验名称>",
  "validation_scope": "<必填: 验证范围（如 NaN隔离防护验证 / 多版本EMA输出一致性验证）>",
  "validation_tool_version": "<必填: 验证脚本/工具版本号>",
  "data_version": "<必填: 数据版本号>",
  "code_version": "<必填: Git commit hash / tag 或 工作副本说明>",
  "executor": "<必填: 执行人>",
  "qa_reviewer": "[可选: QA验证人]",
  "completed_time": "<必填: ISO8601 +08:00>",
  "acceptance_criteria_version": "<必填: 验收标准版本号>",
  "report_type": "verification",
  "verification_type": "<功能验证 / 一致性验证 / 安全边界验证 / 性能验证>"
}
```

---

## 1. 验证结论与核心指标

### 1.1 验证结论

> **<必填: 一句话结论，不超过80字。参考格式：[被测对象]在[验证范围内]，验收标准通过率[X%]，边界测试通过率[Y%]，判定为[PASS/FAIL/...]，置信度[高/中/低]。>**

### 结论判定

| 维度 | 判定 | 说明 |
|:---:|:----:|:----:|
| **最终判定** | `<PASS / PASS_WITH_RESERVATION / FAIL / INCONCLUSIVE>` | 根据 §5.2 优先级规则判定 |
| **confidence** | `<高 / 中 / 低>` | 基于验证覆盖面与复现性 |
| **判定依据** | `<必填: 一句话说明为什么得出此结论>` | |

### 关键假设列表

> 以下为验证结论依赖的核心假设。若假设不成立，结论需重新评估。

| # | 假设 | 影响程度 | 失效风险 | 备注 |
|:-:|:----:|:--------:|:--------:|:----:|
| 1 | `<必填: 关键假设描述（如：验证条件覆盖生产场景）>` | `<高/中/低>` | `<高/中/低>` | `[可选说明]` |
| 2 | ... | ... | ... | ... |
| 3 | ... | ... | ... | ... |

> **假设说明**: 影响程度 = 该假设对最终判定的敏感度；失效风险 = 该假设在生产环境中无法成立的概率。

### 1.2 核心指标表

| 指标名称 | 定义 | 取值范围 | 阈值 | 实测值 |
|:---------|:-----|:---------|:-----|:------:|
| **验收标准通过率** | 通过的验收标准数 / 总验收标准数 | [0,1] | =1.0 为 PASS | `<必填: 数值>` |
| **多版本一致性** | 各实现版本在相同输入下的输出匹配度 | ✅ / ⚠️ / ❌ | ✅ | `<必填>` |
| **边界测试通过率** | 通过的边界用例 / 总边界用例数 | [0,1] | =1.0 为 PASS | `<必填: 数值>` |
| **模式覆盖率** | 已测试的输入模式 / 设计的测试模式 | [0,1] | ≥0.8 为 PASS | `<必填: 数值>` |
| **复现性偏差** | 二次执行与首次结果的输出偏差 | 偏差值（无偏差时=0） | ≤1e-12 | `<必填: 数值>` |
| **执行有效性 (Validity)** | 验证脚本无崩溃/超时（超时阈值：5分钟） | ✅ / ❌ | ✅ | `<必填>` |

---

## 2. 验证配置

### 2.1 验证工具 / 引擎配置

| 配置项 | 值 |
|:------|:---|
| **引擎名称** | `<必填>` |
| **版本** | `<必填>` |
| **精度设置** | `<必填>` |
| **并行度** | `<必填>` |
| **超时设置** | `<必填: 秒/分钟>` |
| **[可选: 其他配置]** | `[可选]` |

### 2.2 被测对象列表

| 函数/模块名 | 源文件路径 | 实现语言/框架 | 版本 | 备注 |
|:-----------|:-----------|:-------------|:----:|:----:|
| `<必填>` | `<必填>` | `<必填>` | `<必填>` | `[可选]` |
| ... | ... | ... | ... | ... |

### 2.3 测试数据配置

| 配置项 | 说明 |
|:------|:-----|
| **数据源** | `<必填: 数据来源及版本>` |
| **构造方式** | `<必填: 合成/真实/混合，构造算法简述>` |
| **测试模式列表** | `<必填: 涵盖的正常/异常/边界模式>` |
| **边界用例列表** | `<必填>` |
| **验收标准版本** | `<必填: 引用验收标准版本号>` |
| **[可选: 其他说明]** | `[可选]` |

---

## 3. 详细验证结果

### 3.1 逐标准验证详情

> 每个验收标准在各被测对象上的通过情况。

| 验收标准ID | 标准描述 | 被测对象 | 结果 | 失败详情 | 备注 |
|:-----------|:---------|:--------:|:----:|:---------|:-----|
| `<CORE-0>` | `<必填: 标准描述>` | `<必填>` | `<PASS/FAIL/NA>` | `[若FAIL: 详细说明]` | `[可选]` |
| ... | ... | ... | ... | ... | ... |

**汇总**: 通过 `<必填: N>` / 总计 `<必填: M>` → 通过率 `<必填: %>`。

### 3.2 模式归因

> 各测试模式（正常/异常/边界）的输出特征分析。

| 测试模式 | 模式描述 | 输入特征 | 输出结果 | 与预期偏差 | 备注 |
|:---------|:---------|:---------|:---------|:-----------|:----:|
| `<模式1>` | `<必填>` | `<必填>` | `<必填>` | `<必填: 偏差值>` | `[可选]` |
| ... | ... | ... | ... | ... | ... |

### 3.3 敏感度分析 [可选]

> 参数变化（如窗口大小、阈值、迭代次数等）对验证结果的影响。

| 参数 | 变化范围 | 对通过率的影响 | 对一致性的影响 | 备注 |
|:----|:---------|:-------------|:--------------|:----:|
| `<参数名>` | `<必填: 范围>` | `<必填>` | `<必填>` | `[可选]` |
| ... | ... | ... | ... | ... |

### 3.4 一致性交叉验证 [可选]

> 多版本/多实现间的输出对比详情。仅适用于对比验证场景。

| 实现版本A | 实现版本B | 输入条件 | 输出差异 | 差异类型 | 判定 |
|:----------|:----------|:---------|:---------|:---------|:----:|
| `<版本>` | `<版本>` | `<条件>` | `<偏差值>` | `<数值/结构/类型>` | `<一致/不一致>` |
| ... | ... | ... | ... | ... | ... |

---

## 4. 风险评估

| 风险类别 | 等级 | 说明 | 应对措施 |
|:--------:|:----:|:----|:---------|
| **覆盖不完整风险** | `<高/中/低>` | `<必填: 测试场景是否覆盖全部生产使用模式>` | `<必填: 应对或补充方案>` |
| **实现偏差风险** | `<高/中/低>` | `<必填: 被测代码在不同环境/平台的行为差异>` | `<必填: 交叉验证或环境约束说明>` |
| **环境依赖风险** | `<高/中/低>` | `<必填: 外部库版本、OS差异导致的潜在行为变化>` | `<必填: CI/CD多环境测试方案>` |
| **边界遗漏风险** | `<高/中/低>` | `<必填: 未测试的极端输入可能导致隐藏问题>` | `<必填: 补充测试计划>` |
| **参数泛化风险** | `<高/中/低>` | `<必填: 仅特定参数验证通过，未确认通用性>` | `<必填: 多参数验证方案>` |
| **[可选: 其他风险]** | `...` | `[可选]` | `[可选]` |

> **等级标准**: 低=风险已被确认消除或验证充分覆盖；中=风险存在但在合理范围内，有针对性应对措施；高=风险可能导致结论不可靠，需进一步验证。

---

## 5. 自检清单（执行方出具）

> 以下由**验证执行方**在提交报告前逐项确认。每项标记 PASS 方可视为报告有效。

| # | 检查项 | 结果 | 确认人 | 备注 |
|:-:|:------:|:----:|:-----:|:----:|
| 1 | **验收标准完整覆盖**：验收标准完整覆盖验证目标，无遗漏 | `<PASS/FAIL>` | `<必填>` | |
| 2 | **测试模式全部执行**：所有预定义测试模式已执行完毕 | `<PASS/FAIL>` | `<必填>` | |
| 3 | **边界用例**：边界用例已覆盖（空输入/极值/异常值） | `<PASS/FAIL>` | `<必填>` | |
| 4 | **多版本一致性**：多版本一致性已验证（如适用） | `<PASS/FAIL/NA>` | `<必填>` | 不适用时标记为NA |
| 5 | **代码版本标注**：代码版本已标注（含无Git管理备选文本说明） | `<PASS/FAIL>` | `<必填>` | 无Git管理时填写"未使用版本管理"或"工作副本（未提交）" |
| 6 | **环境信息写入**：运行环境（OS/Python/依赖库）已记录于附录C | `<PASS/FAIL>` | `<必填>` | |
| 7 | **验证可复现**：种子锁定 / 确定性生成，二次运行可复现 | `<PASS/FAIL>` | `<必填>` | |
| 8 | **审计日志完整**：审计日志已填写，操作记录无缺失 | `<PASS/FAIL>` | `<必填>` | |
| 9 | **[可选: 其他检查项]** | ... | ... | ... |

> **自检规则**: 所有检查项必须为 PASS 或 NA（NA仅限明确不适用的场景且已注明原因）；任一 FAIL 项需附原因说明。

---

## 6. 审计日志

| 时间 | 操作 | 操作人 | 备注 |
|:----:|:----:|:------:|:----:|
| `<ISO8601>` | 执行验证 | `<必填>` | |
| `<ISO8601>` | 填写验证报告 | `<必填>` | |
| `<ISO8601>` | QA验证完成 | `[可选]` | |
| `<ISO8601>` | `[可选: 其他操作]` | ... | ... |

---

## 附录

### 附录 A：关联文档

- **VQA 文件路径**: `<相对路径>/../qa/vqa_{task_id}.md`
- **验证脚本路径**: `<必填: 源代码相对路径>`
- **原始日志路径**: `<必填: 日志文件的相对路径>`
- **[可选: 其他关联文档]**: `[可选]`

### 附录 B：数据构造补充说明 [可选]

> 如需详细描述合成数据生成方式，在此补充。

`[可选: 数据构造算法描述、种子值、统计分布说明等]`

### 附录 C：环境信息

> 运行环境的完整记录，用于复现验证结果。

| 环境项 | 信息 |
|:------|:-----|
| **操作系统** | `<必填: OS名称 + 版本 + 架构>` |
| **Python 版本** | `<必填: Python 版本号>` |
| **NumPy 版本** | `<必填: 版本号>` |
| **pandas 版本** | `<必填: 版本号>` |
| **[可选: 其他依赖]** | `[可选: 版本号]` |

---

*— 文档结束 —*
