---
author: 墨衡
created_time: 2026-05-31T09:14:00+08:00
type: phenomenon_definition_stage1
problem_id: T21_FULLRUN_20260530
version: v1.0
pipeline_stage: 1
---

# 阶段1 现象还原 — T21_FULLRUN_20260530

---

## 执行时间线

| 时间点 | 事件 | 备注 |
|:------|:-----|:-----|
| 2026-05-30 ~14:34 | T+21 黄金基线+编排启动（Phase P0 拆分5子任务 T21_001~005，总估70min） | 任务启动 |
| — | T21_001~003 编码阶段完成 ✅ | — |
| ~16:43 | **第1次全量管线运行启动（marine-s）** | 执行管线计算 + 写库 |
| ~17:00 | **第1次运行被 SIGKILL 终止**，产出222截面/4,293行，被杀时处理到2011-10-14 | 运行持续~17min |
| ~17:00~17:27 | marine-s 与 mellow-o 之间间隔 27min，marine-s RSS 应已释放 | 无并发运行 |
| ~17:27 | **第2次全量管线运行启动（mellow-o）**，使用 INSERT OR IGNORE（自动跳过 marine-s 已有的222截面） | 运行启动 |
| ~17:42 | **第2次运行被 SIGKILL 终止**，产出428截面/7,731行，被杀时处理到2026-05-19 | 运行持续~15min |
| ~17:42后 | 使用 `--no-run` 参数跳过管线计算，直接从已有数据出报告 | 第3次运行，跳过管线 |
| — | 第3次（--no-run）正常完成，验证报告正常输出 ✅ | — |
| ~17:42~18:12 | **后台未关闭的历史验证脚本持续向 a50_cross_ic_result 表写入新截面** | 持续写入中 |
| ~18:12 | **墨涵首次全量审计**：a50_cross_ic_result 表 612行/610唯一截面/~426有效截面（rank_ic NOT NULL）| 全量审计 |
| — | momentum_20d 基线验证：431总窗口/415有效窗口，verdict=FAIL | 基线判定 |
| — | 估值4因子 IC 全为 NULL 或 0 | 数据源问题发现 |
| ~19:50 | **二次采样**：a50_cross_ic_result 表增至 792行/789唯一截面/~774有效截面 | 持续写入中 |
| ~19:55 | **后台写入进程关闭**，数据库最终状态：792行/789唯一截面/~774有效截面 | 管线关闭 |

### 数据快照时间点关系

```
管线运行期                       审计窗口期                  最终静态
16:43 ── 17:00 ── 17:27 ── 17:42 ── 18:12 ── 19:50 ── 19:55 ──→
│marine-s│ 间隔27min │mellow-o│--no-run│全量审计│二次采样│ 关闭
被杀       │         被杀     │        │        │        │
           │                 │        │        │        │
           │                 │        │        │        │
写入进度: 222截面/4,293行    428截面/7,731行 610唯1截面  789唯一截面  最终静态: 789唯一截面
```

**关键快照说明：** 本文档中问题1的逐运行截面数（222/428）为管线运行**被杀时的中间态快照**（17:00/17:42），问题2的基线判定数据（610唯1截面/431 momentum_20d总窗口/415有效窗口）基于**18:12快照**。两套数字时间点不同，不可直接加减比较。

---

## 基准参考线

### 参考线1：管线完整运行预期

| 指标 | 正常预期 | 实际值 | 偏差幅度 | 说明 |
|:----|:--------:|:-----:|:--------:|:----|
| 管线单次运行完成 | 一次运行完整覆盖2007-01~2026-05全量截面 | 两次运行均被SIGKILL终止 | 100%失败 | 两次全量运行均未走完完整管线生命周期 |
| 单次运行截面覆盖 | 完整全量范围 | 第1次222截面（2007-01~2011-10），第2次428截面（2007-01~2026-05） | 第1次覆盖率~52%，第2次覆盖率100% | 第2次实际写库完成全量范围，被杀发生在写库后处理阶段 |
| 管线运行次数 | 1次 | 2次完整 + 1次(--no-run) | +200% | 因SIGKILL需要重跑 |

### 参考线2：黄金基线判定预期

| 指标 | 正常预期阈值 | 实际值 | 偏差幅度 | 比较窗口 |
|:----|:-----------:|:-----:|:--------:|:--------|
| mean_ic_abs | ≥0.02 | 0.221 | +1005%（远超阈值） | 431总窗口/415有效窗口，2007-01~2026-05 |
| positive_ratio | ≥0.50 | **0.487** | -2.6%（略低于阈值） | 同上 |
| ic_std | ≤0.20 | **0.271** | +35.5%（远超阈值上限） | 同上 |
| significant_ratio | ≥0.30 | **0.248** | -17.3%（低于阈值） | 同上 |
| half_life（周） | [4, 12] 周 | **0.47周** | -88.3%（远低于下限） | 同上 |

**综合：** 5项指标仅1项通过（mean_ic_abs），4项未通过 → **verdict=FAIL**

### 参考线3：估值因子数据完整预期

| 指标 | 正常预期 | 实际值 | 偏差幅度 | 比较窗口 |
|:----|:--------:|:-----:|:--------:|:--------|
| PE_TTM 有有效IC的截面比例 | >90%（参考其他因子） | 0%（全部NULL） | -100% | 全表798条因子记录（pe_ttm），rank_ic NOT NULL = 0 |
| PB 有有效IC的截面比例 | >90% | 0%（全部NULL） | -100% | 全表798条（pb），rank_ic NOT NULL = 0 |
| PS_TTM 有有效IC的截面比例 | >90% | 0%（全部NULL） | -100% | 全表798条（ps_ttm），rank_ic NOT NULL = 0 |
| PCF_TTM 有有效IC的截面比例 | >90% | 0%（全部NULL） | -100% | 全表798条（pcf_ttm），rank_ic NOT NULL = 0 |
| stock_daily 表估值字段非空率 | >90% | **0%**（全部NULL） | -100% | 2007~2026全生命周期~138k行数据 |

---

## 现象清单

### 问题组A：管线运行异常（SIGKILL/OOM）

| # | 现象 | 类型 | 数据来源 | 时间点 | 备注 |
|:--|------|:----:|:---------|:------|:-----|
| P1 | 第1次全量管线运行（marine-s）被SIGKILL终止 | 实测 | 管线执行日志 | ~17:00 | 运行持续~17min |
| P2 | 第1次被杀时产出222截面/4,293行，处理到2011-10-14 | 实测 | 管线日志（进度计数） | ~17:00 | ⚠️ 截面数来自日志实时进度，不可通过DB交叉验证 |
| P3 | 第1次被杀时正在写入 a50_cross_ic_result 表，未进入验证阶段 | 实测 | 管线日志 | ~17:00 | INSERT OR IGNORE 逐截面提交，已写入部分已持久化 |
| P4 | 第2次全量管线运行（mellow-o）被SIGKILL终止 | 实测 | 管线执行日志 | ~17:42 | 运行持续~15min |
| P5 | 第2次被杀时产出428截面/7,731行，覆盖2007-01-05~2026-05-19 | 实测 | 管线日志 + DB全量审计 | ~17:42 | ⚠️ 截面数来自日志。DB确认最大日期=2026-05-19，中间无缺口 |
| P6 | 第2次实际已完成所有428截面的写库（覆盖全量范围），杀死发生在写库完成后的后处理阶段 | 推断 | DB全量审计（确认2026-05-19为终点） + 管线日志 | ~17:42 | 验证依据：DB全量范围已覆盖，非"写到一半被中断" |
| P7 | 第2次使用 INSERT OR IGNORE，自动跳过 marine-s 已有的222截面，唯1新增206个截面 | 实测 | 管线代码（INSERT OR IGNORE设计） | — | 去重逻辑确认 |
| P8 | 第3次使用 `--no-run` 跳过管线计算，直接输出报告，正常完成 ✅ | 实测 | 管线执行记录 | ~17:42后 | 验证阶段正常 |
| P9 | 两次运行间隔27分钟（marine-s ~17:00结束，mellow-o ~17:27启动），不并发 | 实测 | 管线执行日志时间戳 | ~17:00~17:27 | — |
| P10 | 事后查询系统可用内存仅1.76GB/15.6GB（总内存15.6GB） | 实测 | Owner查询系统环境 | 18:59（事后） | ⚠️ 非被杀时刻实时快照，仅作为事后环境参考 |
| P11 | 两次SIGKILL发生时间均在运行启动后~15-17min，模式符合 OOM killer 典型行为 | 推断 | 运行时间记录 | ~17:00/17:42 | 缺乏被杀时刻实时内存快照（RSS+系统可用内存），此为模式推断 |
| P12 | 第2次运行启动时 marine-s 已结束27分钟，RSS应已释放 | 推断 | 执行时间线 | ~17:27 | 合理推断，但缺乏marine-s死后的实际内存回收确认 |

### 问题组B：黄金基线 FAIL

| # | 现象 | 类型 | 数据来源 | 时间点 | 备注 |
|:--|------|:----:|:---------|:------|:-----|
| P13 | momentum_20d 黄金基线 verdict=FAIL（5项指标中仅1项通过） | 实测 | `baseline_momentum20d.json` | ~18:12（审计+验证） | 数据基于18:12快照 |
| P14 | positive_ratio=0.487 < 阈值0.50，未通过 | 实测 | 同上 | ~18:12 | — |
| P15 | ic_std=0.271 > 阈值0.20，未通过 | 实测 | 同上 | ~18:12 | 超出阈值35.5% |
| P16 | significant_ratio=0.248 < 阈值0.30，未通过 | 实测 | 同上 | ~18:12 | 低于阈值17.3% |
| P17 | half_life=0.47周，既不满足验证脚本 [4,12] 周区间约束，也不满足需求文档 >12周的单边下限 | 实测 | 同上 | ~18:12 | 阈值本身存在矛盾（脚本[4,12] vs 需求文档>12），但0.47周无论按哪套均FAIL |
| P18 | mean_ic_abs=0.221 ≥ 阈值0.02，通过 ✅ | 实测 | 同上 | ~18:12 | 唯一通过项 |
| P19 | 验证脚本使用去重后的610唯1截面中取 momentum_20d 的 431总窗口/415有效窗口（rank_ic NOT NULL）| 实测 | 验证脚本 `validate_golden_baseline.py` + DB审计 | ~18:12 | 三层过滤：610唯1截面→momentum_20d 431截面→415有效窗口 |
| P20 | 占用重复截面3个（2007-02-02/09/16），来源为 v1管线 + t21_acceptance测试各一行 | 实测 | DB审计 | ~18:12 | 验证脚本内部 drop_duplicates('trade_date') 处理 | 
| P21 | 16个 momentum_20d 截面 rank_ic=NULL（因 num_stocks<30 或因子计算异常） | 实测 | DB查询 | ~18:12 | 已被 rank_ic.dropna() 排除 |
| P22 | 后台未关闭的验证脚本在18:12~19:55期间持续向 a50_cross_ic_result 写入新截面 | 实测 | 墨涵确认（两次时间点行数增长） | 18:12~19:55 | 行数从612增长至792，始终未被关闭 |
| P23 | 18:12快照的基线判定结论（verdict=FAIL）在最终静态数据库上未重算验证 | 不确定 | 墨涵确认 | 18:12（判定基准） | 最终789截面下判定结论的稳定性需重算验证 |
| P24 | 验证脚本中 half_life 的阈值约束为 [4,12] 区间，但需求文档 req_draft_v2 §4.3.2 定义为 >12周单边下限，两者不一致 | 实测 | 代码（validate_golden_baseline.py:49）vs 需求文档 | 代码编写时（非本次事件） | 0.47周无论按哪套均为FAIL，不影响 verdict 结论 |

### 问题组C：估值因子IC=0（数据源可用性问题）

| # | 现象 | 类型 | 数据来源 | 时间点 | 备注 |
|:--|------|:----:|:---------|:------|:-----|
| P25 | a50_cross_ic_result 表中 PE_TTM/PB/PS_TTM/PCF_TTM 4个估值因子的 rank_ic 全部为 NULL 或 0（全生命周期） | 实测 | DB审计 a50_cross_ic_result 表 | ~18:12 | 4因子各798条记录，rank_ic NOT NULL = 0 |
| P26 | 数据源 a50_daily_ohlcv 表中 pe、pb 列存在但100%为NULL（206,387行全部为NULL） | 实测 | DB审计 a50_daily_ohlcv 表结构及数据 | ~18:12（事后查询） | 估值因子管线从该表读取数据 |
| P27 | 数据源 a50_daily_ohlcv 表中 ps_ttm、pcf_ttm 列定义缺失（列不存在） | 实测 | DB审计 a50_daily_ohlcv 表结构（PRAGMA table_info）| ~18:12 | 管线读取时 `available_extra` 为空 |
| P28 | stock_daily 表中 pe、pb、pe_ttm 三列存在但100%为NULL，覆盖2007~2026全生命周期 | 实测 | DB审计 stock_daily 表分年统计 | ~18:12 | ~138k行数据，3列非空均为0 |
| P29 | stock_daily 表中不存在 ps_ttm 或 pcf_ttm 列定义 | 实测 | DB审计 stock_daily 表结构 | ~18:12 | Schema设计阶段未纳入 |
| P30 | 管线 Step 1 对估值因子的 calculate() 正常执行未报错，但返回的因子值均为 None（因数据源字段全空） | 实测 | 管线源码 cross_sectional_ic_pipeline.py 分析 | — | 非跳过计算，非静默失败 |
| P31 | 管线 Step 3~4 对估值因子的 IC 计算正常执行，但因有效股票数为0（n_stocks=0），全部写入 rank_ic=NULL 的记录 | 实测 | 管线源码分析 | — | 管线行为正常，写入逻辑明确 |

---

## 不确定项

| # | 不确定内容 | 原因 | 影响 |
|:--|:----------|:-----|:-----|
| U1 | **被杀时刻的真实内存使用情况** | 缺乏实时内存快照（系统总内存/可用内存、管线进程RSS），事后查询1.76GB/15.6GB为事后环境快照，非SIGKILL时刻 | "OOM为主因"目前是推断性结论，无法确认为实证结论 |
| U2 | **两次 SIGKILL 的确切原因** | 虽然模式符合 OOM killer 典型行为（运行后~15min被杀），但无系统日志或 OOM killer 的 dmesg 记录可证实。其他因素（cgroup限制、进程优先级等）无法排除 | 根因分析的不确定起点 |
| U3 | **marine-s 被杀时系统可用内存是否已极低** | 缺乏 marine-s 被杀时刻的快照。第1次被杀时（~17:00）的资源状态无法回溯 | 不确定第1次是否为OOM导致 |
| U4 | **第1次 marine-s 的222截面数是否精确** | 截面数来自管线日志的实时进度计数。两次运行共享相同 source_version='v1'，无法通过DB查询独立区分两次写入量 | 222截面数不可通过DB交叉验证 |
| U5 | **第2次 mellow-o 的428截面数是否精确** | 同上（截面数来自日志实时计数）。DB中唯1截面共610个，但两段管线写入比例仅能从日志推断 | 428截面数不可通过DB交叉验证 |
| U6 | **最终数据库状态下（789截面）基线判定是否仍为 FAIL** | 基线判定基于18:12快照的431 momentum_20d 总窗口/415有效窗口。最终静态数据库为789唯1截面，若新增截面中 momentum_20d 的 rank_ic 值系统性偏优，理论上有可能改变 verdict。但概率较低（positive_ratio、ic_std 等偏差幅度较大） | 建议在最终静态数据库上重算验证 |
| U7 | **18:12~19:55期间写入的新截面是否全部为独立新增（非覆盖已有）** | INSERT OR IGNORE 设计保证不覆盖已有行，但不确定后台进程执行的SQL是否确实使用INSERT OR IGNORE | 不改变18:12快照已确定的基线判定结论 |
| U8 | **验证脚本中 half_life 阈值应使用哪套标准** | 代码使用 [4,12] 区间，需求文档使用 >12 单边下限，两者矛盾且未经Owner统一 | 需Owner裁决统一标准。当前不影响FAIL结论 |

---

## 边界外事项

以下事项明确**不在本问题的范围内**：

| # | 事项 | 理由 |
|:--|:-----|:-----|
| B1 | T+14 IC管线（18子任务）的架构设计审查 | 本次问题起于T+21运行，T+14已于14:34闭环，其架构不在本次审计范围 |
| B2 | T+21之前的管线编码实现质量（T21_001~003） | T21_001~003 编码阶段已通过 ✅，不在本问题范围内 |
| B3 | akshare ETL 基础数据采集的设计决策 | 估值字段缺失的根因向上追溯至数据源采集层面，但ETL的设计决策（为什么写NULL/为什么没有ps_ttm列）属于上游问题，非本次管线运行异常的范围 |
| B4 | 运行环境（vLLM推理服务等）的内存占用审计 | 其他进程的内存使用是OOM嫌疑参考，但详细审计其他服务的内存占用属于环境基建范畴，非本问题范围 |
| B5 | 验证脚本 `validate_golden_baseline.py` 开发阶段的质量问题 | half_life 阈值矛盾属于脚本开发阶段的历史问题，非本次T+21运行事件所致 |
| B6 | 数据库表结构设计的合理性（为什么 stock_daily 没有 ps_ttm/pcf_ttm 列） | Schema 设计决策属于建表阶段，不在当前管线运行异常的范围内 |
| B7 | 18:12~19:55后台写入进程的具体身份和启动原因 | 该进程的存在是已观察到的现象，但它的启动时间和所属任务不属于T+21全量运行管线的设计范围 |

---

## 自检确认

| 检查项 | 通过 | 说明 |
|:-------|:---:|:------|
| 无原因分析 | ✅ | 全文不包含"因为/导致/原因是"类表述 |
| 无解决方案 | ✅ | 全文不包含"应该/建议/修复/补丁"类表述 |
| 类型标注完整 | ✅ | 每条现象已标注 实测/推断/不确定 |
| 数字可追溯 | ✅ | 每个数字附有数据来源和时间点 |
| 不确定项非空 | ✅ | 8项不确定内容已逐条列出 |
| 边界外事项明确 | ✅ | 7项边界外事项已标注 |
| 基准参考线标注 | ✅ | 3组基准参考线已列出（管线完整运行预期、基线判定预期、估值数据完整预期） |
