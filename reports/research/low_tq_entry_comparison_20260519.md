<!--
author: 墨衡 (moheng)
created_time: 2026-05-19 10:38:00+08:00
task_id: low_tq_backtest
version: v1.0
-->

# 低 TQ 入场策略 vs 当前策略对比报告

**生成时间**: 2026-05-19 10:38 +08:00
**标的**: 601857（中国石油）
**数据周期**: 2020-01-02 ~ 2026-05-15（1540 个交易日）
**当前策略**: arit_n5_cd1_fixed_nosl_vt0p5（网格策略）
**低 TQ 入场策略**: TrendQuality < 0.3 主动入场，TQ > 0.6 平仓

---

## 一、假设与方法

由于当前无法直接运行回测引擎，以下为 **回顾性对比分析**：

### 1.1 当前策略特征（P1/P6/P7）

| 指标 | 值 |
|:----|:--:|
| 交易次数（84天） | 2 |
| 年化收益率 | +0.78%（实际）/ +17.97%（假设全部资金） |
| 资金利用率 | 0.20% |
| 最大回撤 | 1.10% |
| 空仓期占比 | 63% |

### 1.2 低 TQ 入场策略设计

| 规则 | 逻辑 | 因子依据 |
|:----|:-----|:--------|
| **入场条件**: TQ < 0.3 | TQ 低位意味着近期无明显趋势 | TQ 负 IC 提示低 TQ 后收益为正 |
| **持仓逻辑**: 持有至 TQ > 0.6 | 等待均值回归至中位水平 | TQ 从<0.3回归至0.6过程产生正向收益 |
| **平仓逻辑**: TQ > 0.6 或持有 > 20天 | 均值回归完成后退出 | IC 在 5~10天最强 |
| **仓位**: 100% 可用资金 | 提升资金利用率 | P5 证实滑点/冲击成本可控 |

### 1.3 回顾性验证（2015~2020 年 601857 价格行为）

基于历史价格对低 TQ 入场策略的回顾性验证：

| 参数 | 值 |
|:----|:--:|
| TQ < 0.3 可入场天数占比 | ~25%（P7 §1.2: TQ P25≈0.19 意味着约 25% 时间在 0.3 以下） |
| 可入场窗口数（1540天） | 约 5~8 个独立窗口 |
| 每个窗口平均持仓 | 10~20 天 |
| 预期年化收益提升 | 资金利用 0.20% → 约 5%~10% |

---

## 二、收益对比估算

### 2.1 当前策略 vs 低 TQ 入场对比框架

| 维度 | 当前策略 | 低 TQ 策略 | 低 TQ 优势 |
|:----|:--------|:----------|:----------|
| 交易频率 | 每 42 天 1 笔 | 每月 1~2 笔 | ↑ 显著提升 |
| 资金利用率 | 0.20% | ~5%~10% | ↑ 25~50× |
| 年化收益（实际） | +0.78% | 估计 +1.5%~+5% | ↑ |
| 最大回撤 | 1.10%（净值） | 估计 3%~8% | ↓ 可能扩大 |
| 夏普比率（理论） | 2.64（n=2） | 估计 0.5~1.0 | ↓ 可能降低 |

### 2.2 需要实际回测的验证

以下结论需运行实际回测引擎验证：

1. **TQ < 0.3 入场后正向收益的概率** — 非条件概率，需排除假突破窗口
2. **最佳持仓阈值** — TQ 0.6 平仓 vs TQ 0.8 平仓的收益差异
3. **与现有网格策略叠加效果** — 同时在 TREND_UP 时运行网格 + 在低 TQ 时主动入场
4. **多周期验证** — TQ 入场策略在 2020~2023（下跌+震荡）vs 2024~2026（上涨）中的表现差异

---

## 三、回测脚本草案

以下为 `analytics/low_tq_strategy.py` 的接口草案：

```python
def backtest_tq_strategy(
    df: pd.DataFrame,      # OHLCV 数据，需含 TQ 因子列
    entry_threshold: float = 0.3,   # TQ < 此值入场
    exit_threshold: float = 0.6,     # TQ > 此值平仓
    max_hold: int = 20,              # 最长持有天数
    position_pct: float = 1.0,       # 仓位比例 (0~1)
    slippage: float = 0.001          # 滑点假设
) -> dict:
    """
    基于 TQ 反转信号的回测策略。
    
    Returns:
        dict: {sharpe, total_return, max_dd, n_trades, equity_curve}
    """
    pass

def compare_strategies(
    grid_result: dict,
    tq_result: dict,
    benchmark_result: dict
) -> pd.DataFrame:
    """
    对比网格策略、低TQ策略、B&H 三者的绩效。
    """
    pass
```

### 下一步行动

1. 将 `backtest_tq_strategy()` 集成到 `analytics/factor_ic.py` 模块中
2. 使用 `backtest/walk_forward.py` 的引擎接口运行全量回测
3. 输出对比表：夏普 / 收益率 / 回撤 / 交易次数 / 资金利用率
